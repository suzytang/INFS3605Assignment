<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.infs3605assignment">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApp">
        <activity android:name=".ui.knowledge.activity.TrueFalseActivity"></activity>
        <activity android:name=".ui.knowledge.activity.PasswordActivity" />
        <activity android:name=".ui.knowledge.MCQResults" />
        <activity android:name=".ui.knowledge.LearnActivity" />
        <activity android:name=".ui.knowledge.MCQActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>